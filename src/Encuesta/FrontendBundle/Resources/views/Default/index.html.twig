{% extends '::base-frontend.html.twig' %}
{% block contenido %}
{#<div id="fb-root"></div>
<fb:login-button show-faces="true" width="200" max-rows="1"></fb:login-button>
<br />

{% if app.user %}
    {{ app.user.username }} , {{ app.user.usuarioRolesString}}   
    <ul>
        <li><a href="{{ path('frontend_votaciones') }}">Votaciones</a></li>
        <li><a href="{{ path('frontend_perfil') }}">Mi Cuenta</a></li>
        <li><a href="{{ path('frontend_logout') }}">Salir</a></li>
     
    </ul>    
{% else %}
    <a href="{{ path('frontend_login') }}" >Login</a>
    <a href="{{ path('frontend_registro') }}" >Registro</a>
    {#<p>{{ facebook_login_button({'autologoutlink': true}) }}</p>#}
{#{% endif %}
    
 <br />
 <br />
 
 <h3>Categorías</h3>
 <br />
  <ul>
{% for categoria in categorias %}
    <li>{{ categoria.nombre }}</li>
{% endfor %} 
 </ul>
<br />
 <br />
 
 <h3>Eventos Mas Votados</h3>
 
 <br />
 <br />
 
 <ul>
 {% for evento in votados %}
         <li>
            <table width="1000" >
                <tr>
                    <td width="20%"><img src="{{ evento.imagenEvento }}" /></td>
                    <td width="20%">{{ evento.titulo }}</td>
                    <td width="20%" align="center">{{ evento.creador.nombre }}</td>
                    <td width="20%">{{ evento.createdAt | date('d/m/Y H:i') }}</td>  
                    <td><a href="{{ path('frontend_evento', {'id':evento.id}) }}">Ver/Votar</a></td>
                </tr>
            </table>        
        </li>
 {% endfor %}
 </ul> #}

    <!-- Zona Promo -->
    <div id="zona_promo">
    	<div id="img_negocio"></div>
    	<div id="promocion">
        No dejes que un ordenador te diga cuales </br>son los mejores sitios de internet</br>
        <b>¡TÚ DECIDES CON TU Voting!</b>        
        </div>
        <a href="#"><div id="vota"> <div id="tvota">¡Vota!</div> </div></a>
        <a href="#"><div id="creaV"> <div id="tcreaV">Crea tu Voting</div> </div></a>
    	<a href="#"><div id="comparteV"> <div id="tcreaV">Comparte</div> </div></a>
        <div class="limpia_float"></div>        
    </div><!-- Fin Zona Promo -->
    
    <!-- Zona Pestañas -->
    <div id="contenido">
        <div id="pestannas">
            <div id="pest_home" data-img="{{ asset('images/barra_home.png') }}" data-content="home" class="cambiar_pestana pest_activa">Home</div>
            <div id="pest_masV" data-img="{{ asset('images/barra_masV.png') }}" data-content="mas_voting" class="cambiar_pestana">¡Más Voting!</div>
            <div id="pest_loultimo" data-img="{{ asset('images/barra_loultimo.png') }}" data-content="ultimo" class="cambiar_pestana">Lo Último</div>
            <div id="pest_categ" data-img="{{ asset('images/barra_categ.png') }}" data-content="categoria" class="cambiar_pestana">Categorías</div>
            <div class="barra_pest" class="cambiar_pestana"></div>
        </div>
        <div id="cuerpo_cont">
            <div class="cajas"></div>
            <div id="load_cajas" style="text-align: center; padding: 20px 0px; display: none;">
                <img src="{{ asset('images/ajax-loading.gif') }}"/>
            </div>
            <div id="design">DESIGN TEEMSTUDIOS.COM 2014</div>
            <div class="limpia_float"></div>
        </div>
    	     
    </div><!-- Fin Zona Pestañas -->
<!-- fin de cuerpo -->

 {% endblock %}